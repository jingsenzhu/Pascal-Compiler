#include "ASTopt.hpp"

using namespace spc;

int ASTopt::computeBoolExpr(const std::shared_ptr<ExprNode>& expr)
{
    if (is_ptr_of<ConstValueNode>(expr))
    {
        auto c = cast_node<ConstValueNode>
    }
    else
    {
        /* code */
    }
    
}